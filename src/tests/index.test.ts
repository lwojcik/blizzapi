import * as BlizzAPI from '../index';

/* tslint:disable no-expression-statement */
describe('Index', () => {
  test('should export BlizzAPI class', () => {
    expect(BlizzAPI).toBeDefined();
  });
});
/* tslint:enable no-expression-statement */
